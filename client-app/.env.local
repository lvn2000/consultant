# Local overrides for client-app
# This file is gitignored - safe for local development

# When using start-https.sh, point to one of the backend instances
NUXT_PUBLIC_API_BASE=http://localhost:8081/api
